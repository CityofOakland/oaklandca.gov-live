{% if currentUser %}<!-- Begin {{ _self }} -->{% endif %}
<div class="flex">
    <dt class="w-32 font-bold">
      {% switch phone.type %}
      {% case "other" %}
      {{ phone.numberTitle }}
      {% default %}
      {{ phone.getType().name }}
    </dt>
    <dd class="flex-1">
      <a href="tel:{{ phone.number }}">{{ phone.number }}</a>
    </dd>
  {% endswitch %}
</div>
{% if currentUser %}<!-- End {{ _self }} -->{% endif %}