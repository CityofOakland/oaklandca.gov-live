{% if currentUser %}<!-- Begin {{ _self }} -->{% endif %}
{% set link = link ?? "" %}
{% if link %}
  {% include '_components/_button.twig' with {
    class: 'mt-0 mr-0',
    url: link.url,
    target: link.target,
    text: link.text,
  } %}
{% endif %}
{% if currentUser %}<!-- End {{ _self }} -->{% endif %}