{% do craft.app.elements.eagerLoadElements(
    className(entry),
    [entry],
    [
      'documentFile'
    ]
) %}

{% set document = entry.documentFile ? entry.documentFile[0] : null %}

{% if not document %}
  {% exit 404 %}
{% else %}
  {% redirect document.url %}
{% endif %}