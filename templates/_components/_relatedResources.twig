<hr style="border-top: 2px solid #212121;opacity:1;"/>
<h4>{{ block.heading ? block.heading : 'Related Resources' }}</h4>
<ul class="related">
	{% for entry in block.resources.all() %}
		<li><a href="{{ entry.url }}">{{ entry.title }}</a></li>
	{% endfor %}
</ul>