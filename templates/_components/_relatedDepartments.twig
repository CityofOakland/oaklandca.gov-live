{% if block.departments|length %}
  {% include '_components/partials/_cards' with {
    background: '#334E68',
    class:      'text-white',
    heading:    (block.heading ? block.heading : "Related Departments"|t),
    entries:    block.departments.all()
  } %}
{% endif %}