{% if currentUser %}<!-- Begin {{ _self }} -->{% endif %}
{% if block.relatedDepartments|length %}
    {% include '_components/partials/_cards.twig' with {
        background: '#334E68',
        class:      'text-white',
        heading:    "Related Departments",
        entries:    block.relatedDepartments.all()
    } %}
{% endif %}
{% if currentUser %}<!-- End {{ _self }} -->{% endif %}