{% if currentUser %}<!-- Begin {{ _self }} -->{% endif %}
{% set download = entry.agendaDownloadableCopy.one() ?? null %}

<h2>Agenda</h2>
{% if download %}
	<ul class="ul-icons related pt-4">
		<li>
			<a href="{{ download.url }}">
				{{ svg("@webroot/dist/img/icon-document.svg") }} {{ download.title }} - {{ entry.postDate|date("m/d/Y") }} Agenda
			</a>
		</li>
	</ul>
{% endif %}
<h3>Agenda Items</h3>
{{ include("_components/_process.twig") }}
{% if currentUser %}<!-- End {{ _self }} -->{% endif %}
